org.gradle.jvmargs=-Xmx1536m
# We are not using any support library / androidx artifacts at all,
# so keep both of these off to avoid ever pulling in code that assumes
# a higher minSdkVersion.
android.useAndroidX=false
android.enableJetifier=false
